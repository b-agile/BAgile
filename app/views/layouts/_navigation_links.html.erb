<%# add navigation links to this file %>
<% if user_signed_in? %>
    <li class="pull-right"><%= link_to 'Logout', destroy_user_session_path, :method => 'delete' %></li>
<% else %>
    <li><%= link_to 'Login', new_user_session_path %></li>
<% end %>
<% if user_signed_in? %>
    <li class="pull-right"><%= link_to 'Edit account', edit_user_registration_path %></li>
<% else %>
    <li><%= link_to 'Sign up', new_user_registration_path %></li>
<% end %>
<% if user_signed_in? %>
    <li>
      <a data-toggle="dropdown" href="#"><%= t 'projects' %></a>
      <ul class="dropdown-menu" role="menu" aria-labelledby="dLabel">
        <% @projects_on_navbar.each do |p| %>
            <li><%= link_to p.name, p %></li>
        <% end %>
        <% if !@projects_on_navbar.empty? %>
            <hr/>
        <% end %>
        <li><%= link_to t('create_new_project'), new_project_path %></li>
      </ul>
    <li><%= link_to 'New Task', new_task_path %></li>
<% end %>
